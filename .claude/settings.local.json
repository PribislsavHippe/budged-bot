{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit --quiet -m 'fix: банковское уведомление о чаевых — берём сумму чаевых, а не заказа *)",
      "Bash(git push *)"
    ]
  }
}
